import { RadiofilterPipe } from './radiofilter.pipe';

describe('RadiofilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RadiofilterPipe();
    expect(pipe).toBeTruthy();
  });
});
